#!/usr/bin/python3
""" checks if the object is an instance """


def inherits_from(obj, a_class):
    """
    A func that return True if object is an instance of a class

    Args:
        item1: object
        item2: a class


    """
    return (isinstance(obj, a_class))
